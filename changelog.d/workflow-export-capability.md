---
category: Added
---

- **Export workflows to a signed bundle** — `buddy_export_workflows` returns a read-only signed copy of one or more workflows, with every export saved via an explicit path or the default export directory.
