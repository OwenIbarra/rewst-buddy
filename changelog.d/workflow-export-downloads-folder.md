---
category: Added
---

- **Tidier workflow export saves** — pointing `buddy_export_workflows` at the Downloads folder itself now saves the signed bundle inside a `Rewst Exports` subfolder, created when missing.
